import React from 'react';

const UserSearch: React.FC = () => {
    return (
        <div>
            <h1>User Search</h1>
            <p>Search for users here.</p>
        </div>
    );
};

export default UserSearch;
